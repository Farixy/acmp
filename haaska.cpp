#include <bits/stdc++.h>

using namespace std;

int main() {
	int N = 100;
	for (int a = 0; a < N; a++)
	for (int b = 0; b < N; b++)
	for (int c = 0; c < N; c++)
	for (int d = 0; d < N; d++) {
	if ((a-b==9) && (c-d==14) && (a+c==12) && (b+d==2)) cout<<"reshesnie est'"; 
	}
	cout<<"resheniy net"; 
	cout<<"asd";
	return 0;
}